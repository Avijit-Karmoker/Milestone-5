let bonus = 20;

function sum(first, second) {
    let result = first + second + bonus;
    if (result > 9) {
        var mood = "happy";
        mood = "fishy";
        mood = "funky";
        mood = "cranky";
        //        console.log(mood);
    }
    //    console.log(day);
    let day = "friday";
    return result;
}

const output = sum(12, 43);
console.log(output);
